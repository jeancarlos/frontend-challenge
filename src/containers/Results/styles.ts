import styled from 'styled-components'
import { Grid, Typography, Button } from '@mui/material'

export const GridStyled = styled(Grid)`
  && {
    margin-top: 76px;
  }
`

export const ProcessText = styled(Typography)`
  && {
    font-size: 26px;
    font-weight: 700;
    text-align: center;
    text-transform: uppercase;
    line-height: 26px;
    padding-top: 76px;
  }
`

export const ButtonStyled = styled(Button)`
  && {
    height: 48px;
    line-height: 48px;
    margin-top: 30px;
    width: 246px;
  }
`
